{
  "mcpServers": {
    "railway": {
      "type": "local",
      "command": "npx",
      "args": ["railway", "mcp"],
      "cwd": ".",
      "env": {
        "RAILWAY_TOKEN": "${RAILWAY_TOKEN}",
        "NODE_ENV": "production"
      },
      "tools": [
        "shell",
        "network",
        "filesystem"
      ],
      "notes": "Conservative default tool list: shell, network, and filesystem. Store RAILWAY_TOKEN as a secret in your CI/runner environment and never commit secrets. If the MCP server needs additional capabilities (process management, etc.), add them explicitly after review."
    }
  }
}
