{
  "mcpServers": {
    "railway": {
      "type": "local",
      "command": "npx",
      "args": ["railway", "mcp"],
      "cwd": ".",
      "env": {
        "RAILWAY_TOKEN": "${RAILWAY_TOKEN}",
        "NODE_ENV": "production"
      },
      "tools": [
        "shell",
        "network"
      ],
      "notes": "Conservative default tool list: shell and network. Store RAILWAY_TOKEN as a secret in your CI/runner environment and never commit secrets. If the MCP server needs additional capabilities (filesystem, process management, etc.), add them explicitly after review."
    }
  }
}
