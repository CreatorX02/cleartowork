# Example environment variables for local testing and CI

# Railway API token (do NOT commit real tokens)
RAILWAY_TOKEN=

# Node environment
NODE_ENV=production

# Add other required variables here (e.g., PORT, DATABASE_URL) as needed.
# Store real secrets in your CI or repository secrets store, not in this file.
